<Window x:Class="EspansoAddon.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EspansoAddon"
        mc:Ignorable="d" WindowStyle="ToolWindow" Background="#a488"
        Title="Espanso Manager" Height="450" Width="500">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="1.4*"/>
			<ColumnDefinition Width="0.5*"/>
			<ColumnDefinition Width="0.5*"/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
			<RowDefinition Height="0.3*"/>
			<RowDefinition Height="0.3*"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<!-- Hidden grid for the star width -->
		<Grid Grid.Column="0" Grid.RowSpan="5" Visibility="Hidden" IsHitTestVisible="False">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="*"/>
			</Grid.ColumnDefinitions>
			<Border x:Name="Col1" Grid.Column="0"/>
			<Border x:Name="Col2" Grid.Column="1"/>
		</Grid>

		<ListView Grid.Column="0" Grid.RowSpan="5" Name="TheList" SelectionMode="Single" HorizontalContentAlignment="Stretch">
			<ListView.View>
				<GridView>
					<GridViewColumn Header="Trigger" Width="{Binding ElementName=Col1, Path=ActualWidth}">
						<GridViewColumn.CellTemplate>
							<DataTemplate>
								<TextBlock Text="{Binding Trigger}" TextWrapping="Wrap" Padding="2"/>
							</DataTemplate>
						</GridViewColumn.CellTemplate>
					</GridViewColumn>
					<GridViewColumn Header="Replace" Width="{Binding ElementName=Col2, Path=ActualWidth}">
						<GridViewColumn.CellTemplate>
							<DataTemplate>
								<TextBlock Text="{Binding Replace}" TextWrapping="Wrap" Padding="2"/>
							</DataTemplate>
						</GridViewColumn.CellTemplate>
					</GridViewColumn>
				</GridView>
			</ListView.View>
		</ListView>

		<Button Name="fileSelect" Content="File" Click="fileSelect_Click"
                Grid.Column="2" Height="20" Width="50" Margin="5"
                VerticalAlignment="Top" HorizontalAlignment="Right"/>

		<TextBlock Name="fileTextBlock" Foreground="White" FontWeight="Bold" FontSize="9"
                   Grid.Column="1" Grid.Row="0" Margin="5" TextWrapping="Wrap" Text="No file selected"/>

		<TextBox Name="tbTrigger" Grid.Column="1" Grid.Row="1" Margin="5" Grid.ColumnSpan="2"
                 VerticalContentAlignment="Center"
                 Tag="Trigger" Text="{Binding TbTrigger, UpdateSourceTrigger=PropertyChanged}"/>

		<TextBox Name="tbReplace" Grid.Column="1" Grid.Row="2" Margin="5" Grid.ColumnSpan="2"
                 VerticalContentAlignment="Center"
                 Tag="Replace" Text="{Binding TbReplace, UpdateSourceTrigger=PropertyChanged}"/>

		<Button Name="Add" Content="Add" Grid.Column="1" Grid.Row="3" Margin="10,40"
                Click="Add_Click" MinHeight="25" />

		<Button Name="Delete" Content="Delete" Grid.Column="2" Grid.Row="3" Margin="10,40"
                Click="Delete_Click" MinHeight="25"/>
	</Grid>
</Window>
